@using Microsoft.AspNetCore.Identity;
@using Microsoft.AspNetCore.Identity.Data;
@using CinemaSchedule.Views.Profile;
@using CinemaSchedule.Areas.Identity.Data
@using Microsoft.EntityFrameworkCore

@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager
@{
    var userAuth = await UserManager.GetUserAsync(User);
}

@model User

<head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="~/css/ProfileStyles.css" asp-append-version="true" />
</head>
<main class="container">
    <h1>Изменение прав сотрудника</h1>
    <form method="post">
        <div class="mb-3">
            <label for="exampleFormControlInput1" class="form-label">Сотрудник</label>
            @{
                string naming = Model.FirstName + " " + Model.LastName;
            }
            <div class="col-sm-12">
                <input type="text" class="form-control" value="@naming" disabled="disabled">
            </div>
        </div>
        <div class="col-6 col-sm-4 ms-lg-3">
        <div class="row align-items-start">
        <div class="form-check form-check-inline">
            <input class="form-check-input" name="checksCountries" type="checkbox" id="Greece" value="Греция">
            <label class="form-check-label" for="flexCheckDefault">Может просматривать</label>
        </div>
        <div class="form-check form-check-inline">
            <input class="form-check-input" name="checksCountries" type="checkbox" id="Greece" value="Греция">
            <label class="form-check-label" for="flexCheckDefault">Может изменять</label>
        </div>
        </div>
        </div>
        <div class="mb-3">
            <button type="submit" class="btn btn-success">Сохранить</button>
        </div>
    </form>
</main>
